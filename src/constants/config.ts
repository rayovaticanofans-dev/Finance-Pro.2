export const STORAGE_KEY = 'fp-v3';
export const LIMIT = 1000;
export const APP_VERSION = '3.0.0';

export const FEATURE_FLAGS = {
  ocr: true,
  ai: true,
  openBanking: false,
  cloudBackup: false,
} as const;

export const API_BASE = '/api/mock';
